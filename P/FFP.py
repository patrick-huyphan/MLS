#!/usr/bin/python
# MLS

def buildBitString(a):
    return b

def intersec(a, b):

    return c
    
def isSub(a, b):
    return c
    
def addZ(PA,z):
    for x in PA:
        if x:
        
    return PA

    
def batchMerge(PA,PB):
    for x in PA:
        for y in PB:
            q = intersec(x,y)        
            if(q):
                if(isSub(x,q)): s1= -1
                if(isSub(y,q)): s2= -1
                for z in M:
                    if(isSub(z,q)):
                        y=x
                        break
                    if(isSub(q,z)):
                        flag = 1
                        break
            else:
                flag = 1
            
            if flag = 0:
                M = M | q;
                
    for x in PA:
        if s1: M = M &x1
    
    for y in PB:
        if s2: M = M &x2
    return M    
    print("hello")
