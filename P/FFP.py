#!/usr/bin/python
# MLS
def addZ(PA,z):
	for x in PA:
		if x:
		
	return PA

def fft(PA,PB):
	for x in PA:
		for y in PB:
			q = x|y		
			if(q):
				if(x in q): s1= -1
				if(y in q): s2= -1
				for z in M:
					if(z in q):
						y=x
						break
					if(q in z):
						flag = 1
						break
			else:
				flag = 1
			
			if flag = 0:
				M = M | q;
				
	for x in PA:
		if s1: M = M &x1
	
	for y in PB:
		if s2: M = M &x2
	return M	
	print("hello")
